PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/women-centric
JWT_SECRET=change_this_to_a_long_random_secret_key
JWT_EXPIRES_IN=7d

# Live frontend URL (no trailing slash) — required for CORS on Render
FRONTEND_URL=https://your-frontend.onrender.com
